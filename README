super simple command tool to render jinja2 templates in the same directory

you need to have jinja2 installed, so:
```
pip install Jinja2
``
or whatever you do to install python modules

usage: jinjathis [-h] [-p PATH]

options:
  -h, --help            show this help message and exit
  -p PATH, --path PATH  path for the file you want to render
